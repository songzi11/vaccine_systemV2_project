# uni-app 项目 AI 自动化测试修复方案报告

> 适用项目：vaccine_systemV2（uni-app 跨端应用）
> 生成日期：2026-04-15
> 工具链：agent-browser + Claude Code

---

## 一、方案目标

实现一个**全自动、带安全护栏**的测试-修复闭环：

1. 自动启动 uni-app H5 开发服务器
2. 用 `agent-browser` 自动执行页面测试、截图、日志抓取
3. 将异常报告传给 AI（Claude Code）
4. AI 自动定位源码、修复 bug、保存文件
5. 自动验证修复效果，循环直至干净或达到止损上限

---

## 二、工具链现状

| 工具 | 状态 | 作用 |
|------|------|------|
| `agent-browser` | 已安装 | 自动打开浏览器、执行页面操作、截图、抓取 console/network |
| Chrome | 已配置 | `agent-browser` 通过 `AGENT_BROWSER_EXECUTABLE_PATH` 调用系统 Chrome |
| `Claude Code` | 已可用 | 读源码、分析报错、自动修改代码 |
| `git` | 项目已初始化 | 版本控制、备份回滚 |

---

## 三、方案架构：四层安全护栏

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  ① 安全准备层   │ ──▶ │  ② 自动测试层   │ ──▶ │  ③ AI 修复层    │
│  (git 自动备份) │     │ (agent-browser) │     │  (Claude Code)  │
└─────────────────┘     └─────────────────┘     └─────────────────┘
         ▲                                               │
         └──────────── ④ 验证 & 止损机制 ◄───────────────┘
```

---

## 四、第 ① 层：安全准备（全自动执行）

每次运行前，脚本自动完成以下动作，无需人工介入：

1. **保存当前工作区**
   - 执行 `git stash` 保存未提交改动
   - 防止覆盖正在手写的代码

2. **创建隔离分支**
   - 执行 `git checkout -b ai-fix-YYYYMMDD-HHMMSS`
   - AI 的所有修改都在独立分支上，不影响 `main` 或 `master`

3. **检查 Source Map 配置**
   - 确认 `manifest.json` 或 `vite.config.js` 中 Source Map 已开启
   - 确保报错能映射回 `.vue` / `.ts` 原始代码

**保险机制**：如果项目没有 git 仓库，脚本会自动 `git init` + `git add .` + `git commit -m "init: AI auto-fix baseline"`。

---

## 五、第 ② 层：自动测试引擎

脚本按标准化检查清单逐项执行测试，生成结构化报告。

### 5.1 测试流程

| 步骤 | 命令示例 | 目的 |
|------|---------|------|
| 1. 启动服务 | `npm run dev:h5` | 启动本地开发服务器 |
| 2. 端口探测 | 脚本轮询 `http://localhost:端口` | 确认服务完全就绪 |
| 3. 首屏检查 | `agent-browser open` + `snapshot` + `screenshot` | 确认页面正常渲染 |
| 4. 错误扫描 | `agent-browser console` | 收集 JS Error / Warning |
| 5. 接口检查 | `agent-browser network requests` | 收集 HTTP >= 400 的请求 |
| 6. 元素存在性检查 | 解析 `snapshot` 输出 | 核对关键元素是否缺失 |
| 7. 基础交互测试（可选） | `agent-browser click @eX` + `snapshot` | 验证核心按钮可点击 |

### 5.2 输出物：`bug-report-YYYYMMDDHHMMSS.md`

报告包含以下内容：
- 测试时间戳与分支名
- 页面截图文件路径
- Console 报错原文与堆栈
- 失败接口列表（URL + 状态码）
- 缺失元素列表（预期存在 vs 实际缺失）
- AI 建议修复优先级：
  - **P0**：页面崩溃 / 白屏 / 核心功能无法使用
  - **P1**：功能异常 / 接口失败 / 交互无响应
  - **P2**：警告信息 / 非阻塞性异常

---

## 六、第 ③ 层：AI 修复引擎

脚本将 `bug-report.md` 提交给 Claude Code，触发标准化修复流程。

### 6.1 Claude Code 的标准动作

1. **过滤噪音**：区分真正的业务报错与第三方库警告
2. **定位源码**：利用 Source Map 将报错行号映射回 `.vue` / `.ts` 文件
3. **输出修复计划**：明确写出"改哪个文件的第几行、原因是什么"
4. **执行修改**：使用 Edit 工具精确修改代码
5. **保存并触发 HMR**：等待编译完成

### 6.2 修复约束

为避免 AI 过度修改，强制遵循以下规则：
- **最小改动原则**：只修改与报错直接相关的代码
- **不重构无关代码**：不改命名、不删注释、不调整结构
- **uni-app 兼容意识**：优先使用 uni-app 跨端 API（如 `uni.request`），避免写死浏览器专属代码

---

## 七、第 ④ 层：验证 & 止损机制（最关键）

### 7.1 硬止损：迭代上限

- 单轮运行最多修复 **3 轮**
- 3 轮后仍有异常 → **自动停止，不再尝试**
- 生成 `report-final.md` 说明：AI 已修了什么、还剩什么问题、建议人工处理方向

### 7.2 坏修改自动回滚

每轮修改后，重新执行第 ② 层测试清单：
- **如果结果比修改前更糟**（报错更多 / 页面更空）→ 自动执行 `git checkout .` 撤销本轮修改
- 如果 3 种修复思路都失败 → 自动切回最初备份分支，完全放弃本轮自动修复

### 7.3 冷启动验证

除等待 HMR 外，每轮修复后要求 `agent-browser` **关闭浏览器后重新打开**页面：
- 防止"热更新时正常，真刷新就崩"的假阳性

### 7.4 变更日志

每轮修改后自动生成 `changes-round-N.md`，记录：
- 修改的文件列表
- 每处改动的具体 diff
- 改动的理由
- 本轮测试验证结果（通过 / 失败 / 回滚）

---

## 八、用户使用方式（极简）

方案落地后，作为用户的你只需做一件事：

```bash
# 方式一：在项目根目录双击运行
auto-fix.bat

# 方式二：在 Claude Code 交互窗口发送标准 Prompt
```

### 8.1 标准 Prompt（复制即用）

```
请帮我自动测试并修复当前 uni-app 项目的 bug。

按以下步骤严格执行：
1. 执行 git stash（如有未提交改动）并创建分支 ai-fix-当前时间戳
2. 运行 npm run dev:h5 启动本地 H5 开发服务器，等待端口就绪
3. 用 agent-browser 打开首页，依次执行 snapshot、screenshot、console、network requests
4. 分析异常：JS 报错、页面空白、关键元素缺失、接口失败（状态码 >= 400）
5. 如有 bug，根据报错精确定位到 .vue / .js / .ts 源码，遵循最小改动原则修复
6. 保存代码后，先等待 3 秒让 HMR 生效，然后关闭浏览器并重新 open 做冷启动验证
7. 再次执行 snapshot + console + screenshot 验证
8. 重复步骤 3-7，最多 3 轮。如果某轮修改后结果比上一轮更差，立即执行 git checkout . 回滚
9. 无论最终是否完全修复，生成 report-final.md 总结全部修改和遗留问题

注意：
- agent-browser 已通过 AGENT_BROWSER_EXECUTABLE_PATH 环境变量配置好系统 Chrome
- 只修与报错直接相关的代码，不要顺手重构无关逻辑
- 每轮修改后必须生成 changes-round-N.md
```

### 8.2 运行结束后你看什么

- **`report-final.md`**：总报告，告诉你修了什么、还有什么没修
- **`changes-round-1.md` 等**：每轮具体改了什么
- **`git diff ai-fix-xxx`**：完整的代码变更对比

---

## 九、方案边界与诚实风险

即使加了 4 层安全护栏，以下问题**仍然无法解决**：

| 风险类型 | 说明 | 当前处理策略 |
|---------|------|-------------|
| **视觉类 bug** | 样式错位、颜色异常、元素被截断 | agent-browser 只能读 DOM 结构，无法感知像素级差异 |
| **时序/竞态 bug** | 快速双击、异步加载顺序导致的偶现问题 | 顺序点击无法模拟复杂交互时序 |
| **小程序/App 端异常** | 本方案只测 H5，wx.xxx API 的兼容问题覆盖不到 | H5 修复后仍需单独在微信开发者工具中验证 |
| **AI 理解错业务逻辑** | AI 按"不报错"修，不一定按"业务正确"修 | 通过"最小改动+变更日志"降低影响，复杂逻辑仍需人工 review |
| **后端问题误判** | 接口 500 可能是后端 bug，AI 可能错误地在前端加容错 | 报告中会标注接口异常，建议人工判断根因 |

---

## 十、扩展预留：未来支持微信小程序

当前架构已为多平台测试预留扩展接口：

| 阶段 | H5（当前已实现） | 微信小程序（后续扩展） |
|------|-----------------|---------------------|
| 启动 | `npm run dev:h5` | 微信开发者工具 CLI |
| 测试 | `agent-browser` | `miniprogram-automator` |
| 修复 | Claude Code 改源码 | 同一套 uni-app 源码 |
| 验证 | 脚本自动刷新浏览器 | 需额外配置开发者工具自动化 |

---

## 十一、下一步行动建议

1. **先手动跑一次标准 Prompt**：在 Claude Code 中发送第 8.1 节的 Prompt，观察第一轮实际效果
2. **确认 H5 核心流程正常**：在手动跑通后再考虑做 `auto-fix.bat` 脚本
3. **审查首份 changes-round-1.md**：确认 AI 的修改风格是否符合预期
4. **逐步增加测试深度**：从"首页不报错"扩展到"核心按钮可点击"再到"表单提交成功"

---

## 十二、结论

本方案**不是一键修完所有 bug 的神器**，而是一个**有安全护栏的自动化辅助工具**。它的真实价值在于：

> 在 git 隔离保护下，让 AI 帮你快速扫一遍 H5 的明显报错，自动尝试修复并给出完整报告，最终把"确定无误"的合并决策权留给你。

**适合的场景**：H5 端 JS 报错、接口联调、元素缺失类问题。
**不适合的场景**：完全无人值守跑生产环境、替代人工测试和代码审查、处理复杂跨端兼容问题。
