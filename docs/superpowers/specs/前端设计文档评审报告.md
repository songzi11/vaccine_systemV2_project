# 疫苗管理系统 — 前端设计文档评审报告

| 项目 | 内容 |
|------|------|
| 文档编号 | REVIEW-REPORT-002 |
| 评审基准 | 前端设计文档评审方案（REVIEW-PLAN-002） |
| 评审日期 | 2026-04-04 |
| 评审范围 | 医生端APP前端设计、家长端APP前端设计、管理员端APP前端设计 |
| 评审基准文档 | 系统架构设计、API接口设计、共享内核、数据库设计 |

---

## 1. 评分总览

### 1.1 三端综合评分

| 维度 | 满分 | 医生端 | 家长端 | 管理员端 | 说明 |
|------|------|--------|--------|----------|------|
| **D1: PRD功能覆盖** | 12 | 12 | 12 | 12 | 三端功能覆盖完整 |
| **D2: 页面流程覆盖** | 12 | 12 | 12 | 11 | 管理端CRUD流程覆盖充分 |
| **D3: 业务规则覆盖** | 11 | 11 | 11 | 10 | 管理端部分PRD规则待确认 |
| **D4: 技术选型一致性** | 8 | 7 | 7 | 7 | 三端均使用uni-ui，与架构uView UI 2.x不一致 |
| **D5: API接口一致性** | 10 | 10 | 10 | 2 | **管理员端API路径系统性错误** |
| **D6: 安全方案一致性** | 9 | 8 | 9 | 7 | 医生端缺少权限码级按钮控制描述 |
| **D7: 数据模型一致性** | 8 | 8 | 8 | 4 | **管理员端状态码/枚举值系统性矛盾** |
| **D8: 路由与页面完备性** | 8 | 8 | 8 | 8 | 三端路由设计完整 |
| **D9: 组件与交互完备性** | 8 | 7 | 8 | 7 | 医生端/管理端空状态描述不够详细 |
| **D10: 异常处理完备性** | 7 | 5 | 6 | 3 | 医生端/管理端缺少404兜底和空状态描述 |
| **D11: 状态管理与轮询完备性** | 7 | 7 | 5 | 5 | 家长端轮询生命周期管理描述不足 |
| **合计** | **100** | **95** | **96** | **82** | |

### 1.2 三支柱汇总

| 支柱 | 满分 | 医生端 | 家长端 | 管理员端 |
|------|------|--------|--------|----------|
| 支柱一：PRD符合性 | 35 | 35 | 35 | 33 |
| 支柱二：架构一致性 | 35 | 33 | 34 | **20** |
| 支柱三：设计完备性 | 30 | 27 | 27 | 23 |
| **总计** | **100** | **95** | **96** | **76** |

### 1.3 等级评定

| 端 | 总分 | 等级 | 结论 |
|----|------|------|------|
| 医生端 | 95 | **S** | 优秀，少量优化后可进入开发 |
| 家长端 | 96 | **S** | 优秀，少量优化后可进入开发 |
| 管理员端 | 76 | **B** | 合格，需修复API路径和数据模型一致性问题 |

---

## 2. 一票否决项检查结果

| 编号 | 否决项 | 医生端 | 家长端 | 管理员端 |
|------|--------|--------|--------|----------|
| V1 | PRD核心功能点完全缺失 | **未触发** | **未触发** | **未触发** |
| V2 | 核心业务流程断裂 | **未触发** | **未触发** | 不适用（管理端无核心流程链） |
| V3 | API路径或HTTP方法矛盾 | **未触发** | **未触发** | **已触发** |
| V4 | 认证流程或权限控制矛盾 | **未触发** | **未触发** | **未触发** |
| V5 | 枚举值/状态码/错误码矛盾 | **未触发** | **未触发** | **已触发** |

### 管理员端否决项详情

**V3（API路径矛盾）**：管理员端API层设计中全部34个端点缺失 `/api/v1` 前缀，且7个模块中有5个模块使用了与API接口设计文档完全不同的路径结构：

| 管理员端前端路径 | API接口设计正确路径 | 差异类型 |
|----------------|-------------------|---------|
| `/api/schedule` | `/api/v1/admin/schedules` | 前缀缺失 + 模块路径不同 |
| `/api/business/window` | `/api/v1/admin/windows` | 前缀缺失 + 模块路径不同 |
| `/api/business/vaccine` | `/api/v1/admin/vaccines` | 前缀缺失 + 模块路径不同 |
| `/api/business/notice` | `/api/v1/admin/notices` | 前缀缺失 + 模块路径不同 |
| `/api/admin/notice/{id}/approve` | `/api/v1/admin/notices/{id}/audit` | 前缀缺失 + 路径不同 + 操作名不同 |
| `/api/admin/stats/*` (POST) | `/api/v1/admin/stats/*` (GET) | 前缀缺失 + HTTP方法不同 |
| `/api/admin/users/{userId}/roles` | `/api/v1/admin/users/{userId}/assign-roles` | 前缀缺失 + 路径不同 |

**V5（枚举值/状态码/错误码矛盾）**：

| 管理员端定义 | 共享内核/API设计正确值 | 差异说明 |
|-------------|---------------------|---------|
| 公告状态：0待审批/1已审批/2已拒绝 | 0待审批/1已发布/2已下架/**3已拒绝** | 缺少"已下架"状态，"已审批"应为"已发布" |
| 公告类型：NOTICE/REMINDER/URGENT | NORMAL/URGENT/SYSTEM | 枚举值完全不同 |
| 排班状态：0正常/1已取消/2已换班 | 0正常/1请假/2已取消 | 状态码语义不同（1和2互换了含义） |
| 窗口职能包含SCHEDULE | 窗口职能无SCHEDULE（仅有SIGNIN/PRECHECK/REGISTER/VACCINATE/OBSERVE） | 多出不存在枚举值 |
| 错误码使用5001-5055区间 | 管理模块错误码应为8000-8999区间 | 与登记模块错误码（5000-5999）冲突 |

---

## 3. 各维度详细评审结果

### D1: PRD功能覆盖（12分）

#### 医生端：12/12

全部PRD功能点均有对应页面：

| PRD模块 | 功能点 | 页面映射 | 状态 |
|---------|--------|---------|------|
| PRD-03 | 签到列表/执行签到 | `queue/signin` + `process/signin-confirm` | ✓ |
| PRD-03 | 预检队列/预检评估 | `queue/precheck` + `process/precheck-assess` | ✓ |
| PRD-04 | 登记队列/登记处理 | `queue/register` + `process/register-process` | ✓ |
| PRD-04 | 叫号管理/过号记录 | `calling/index` + `calling/history` | ✓ |
| PRD-04 | 批次切换 | `process/batch-switch` | ✓ |
| PRD-05 | 接种队列/接种执行 | `queue/vaccinate` + `process/vaccinate-process` | ✓ |
| PRD-05 | 接种成功/接种记录 | `process/vaccinate-success` + `record/list` + `record/detail` | ✓ |
| PRD-05 | 儿童接种史 | `record/child-history` | ✓ |
| PRD-05 | 留观队列/留观监控 | `queue/observe` + `process/observe-detail` | ✓ |
| PRD-05 | 留观结束 | `process/observe-finish` | ✓ |
| PRD-05 | 不良反应上报/处理 | `process/adverse-report` + `process/adverse-handle` | ✓ |
| PRD-06 | 库存总览/批次/调拨/预警/销毁 | `stock/*` (6个页面) | ✓ |

**问题：** 无

#### 家长端：12/12

全部PRD功能点均有对应页面：

| PRD模块 | 功能点 | 页面映射 | 状态 |
|---------|--------|---------|------|
| PRD-01 | 登录（密码+短信验证码） | `auth/login` | ✓ |
| PRD-01 | 注册/忘记密码/修改密码 | `auth/register` + `auth/forgot-password` + `auth/change-password` | ✓ |
| PRD-01 | 儿童档案CRUD | `child/list` + `child/add` | ✓ |
| PRD-02 | 创建/取消/列表/详情预约 | `appointment/create` + `appointment/detail` + `appointment/list` | ✓ |
| PRD-02 | 流程指引 | `appointment/guide` | ✓ |
| PRD-02 | 排队信息 | 预约详情子功能（`guide`页`QueueInfoCard`） | ✓ |
| PRD-06 | 疫苗目录浏览 | `vaccine/list` + `vaccine/detail` | ✓ |
| PRD-07 | 公告列表/详情/反馈 | `notice/list` + `notice/detail` + `notice/feedback` | ✓ |
| PRD-05 | 接种记录查询 | `record/list` | ✓ |

**问题：** 无

#### 管理员端：12/12

覆盖排班管理、窗口管理、疫苗管理、公告管理、角色权限管理、系统配置和统计分析全部功能模块。

**问题：** 无

---

### D2: 页面流程覆盖（12分）

#### 医生端：12/12

| 核心流程 | 链路完整性 | 分支覆盖 | 返回路径 |
|---------|-----------|---------|---------|
| 签到→预检 | ✓ 签到队列→签到确认→预检队列→预检评估 | ✓ 预检不通过（状态码9） | ✓ 各页面有返回按钮 |
| 登记+叫号 | ✓ 登记队列→登记处理→叫号面板→确认到达 | ✓ 过号、超时、3次过号取消 | ✓ |
| 接种 | ✓ 接种队列→接种执行→接种成功→留观提醒 | ✓ 批次过期警告 | ✓ |
| 留观 | ✓ 留观队列→留观详情→留观结束 | ✓ 异常留观→不良反应上报 | ✓ |
| 库存管理 | ✓ 库存总览→批次列表→批次详情→调拨/销毁 | ✓ 批次切换 | ✓ |

**亮点：** 叫号状态机描述清晰（含过号上限、超时自动过号逻辑）。

#### 家长端：12/12

| 核心流程 | 链路完整性 | 返回路径 |
|---------|-----------|---------|
| 预约全流程 | ✓ 创建预约(3步)→预约列表→预约详情→流程指引 | ✓ |
| 流程指引 | ✓ 6步流程图（签到→预检→登记→接种→留观→完成），当前步骤高亮 | ✓ |

**亮点：** 页面导航与传参规范非常完整（§页面导航与传参规范），包含所有页面跳转路径汇总表。

#### 管理员端：11/12

| 流程 | 链路完整性 |
|------|-----------|
| 排班CRUD | ✓ 列表→编辑→冲突检测→提交 |
| 公告发布审批 | ✓ 发布→审批→反馈查看 |
| 窗口管理 | ✓ 列表→编辑→服务配置 |
| 角色分配 | ✓ 列表→编辑→分配用户 |

**问题：**
- **P2 [管理员端]** 统计分析页面间切换逻辑未描述（四个统计子页面如何切换——Tab内嵌还是独立路由跳转）。-1分

---

### D3: 业务规则覆盖（11分）

#### 医生端：11/11

全部14条业务规则均有前端体现：

| 规则 | 前端体现位置 |
|------|------------|
| 签到身份核验（身份证18位+匹配） | `signin-confirm` 页面描述 |
| 预检体温阈值（>37.3°C） | `precheck-assess` 页面，自动判定+红色警告 |
| 预检必填项（体温、健康状况） | `precheck-assess` 页面 |
| 登记核实（4张卡片全勾选） | `register-process` 页面，VerifyCheckbox组件 |
| FEFO批次选取 | `register-process` 页面，自动选批+更换按钮 |
| 叫号过号上限（3次） | `calling/index` 页面，3次自动取消逻辑 |
| 叫号超时（5分钟） | `calling/index` 页面，倒计时 |
| 接种部位必选 | `vaccinate-process` 页面，4按钮组 |
| 接种二次确认 | `vaccinate-process` 页面，确认弹窗 |
| 留观最短时间（30分钟） | `observe-detail` 页面，倒计时 |
| 异常留观须先上报不良反应 | `observe-finish` 页面，校验逻辑 |
| 批次销毁必填原因 | `stock/dispose` 页面 |
| 调拨校验（调出≠调入，数量<=可用） | `stock/transfer` 页面 |
| 防重放（X-Request-Id） | 业务约束表列出 |

**问题：** 无

#### 家长端：11/11

全部7条业务规则均有前端体现：

| 规则 | 前端体现位置 |
|------|------------|
| 手机号格式校验（11位） | 登录/注册页，pattern `/^1[3-9]\d{9}$/` |
| 短信验证码60秒倒计时 | 登录页描述 |
| 密码强度要求 | 注册页，pattern要求字母+数字 |
| 儿童信息必填项 | 添加儿童页，必填校验规则表 |
| 预约提前天数限制（最多7天） | 创建预约页，日历仅未来7天可选 |
| 预约时段选择 | 创建预约页，时段列表+剩余名额 |
| 预约取消条件（仅待签到） | 预约详情页，`status=1` 时才显示取消按钮 |

**问题：** 无

#### 管理员端：10/11

管理员端有详细业务约束表（20条），覆盖排班、窗口、疫苗、公告、角色等模块。

**问题：**
- **P1 [管理员端]** 排班修改/删除的前置条件（"排班日期已过且已有预约的排班不能修改时间"、"已有预约的排班不可删除"）在业务约束中列出，但编辑/列表页面交互设计中未描述如何检查和提示用户（如编辑页加载时判断、删除时弹窗提示原因）。-1分

---

### D4: 技术选型一致性（8分）

#### 三端通用问题：7/8

| 检查项 | 架构设计规定 | 三端实际 | 一致性 |
|--------|------------|---------|--------|
| 框架 | UniApp + Vue 3 (Composition API) | 一致 | ✓ |
| 状态管理 | Pinia | 一致 | ✓ |
| UI组件库 | **uView UI 2.x** | **uni-ui** | **不一致** |
| HTTP请求 | uni.request封装 | 一致（request.js） | ✓ |
| 样式方案 | SCSS + uni.scss | 一致 | ✓ |
| 多端支持 | H5 + 微信小程序 + APP | 一致 | ✓ |
| 项目结构 | §4.1目录规范 | 基本一致 | ✓ |
| 构建工具 | HBuilderX / CLI | 未明确提及 | - |

**问题：**
- **P2 [三端]** UI组件库选用 `uni-ui` 而非架构设计§2.1规定的 `uView UI 2.x`。三份文档均一致使用 `uni-ui`，说明这是有意为之的统一决策，但未在任何文档中说明变更理由。-1分

**补充说明：** 家长端使用原生TabBar，医生端/管理员端使用自定义TabBar（CustomTabBar.vue）。由于家长端角色固定（USER），TabBar不需要动态变化，使用原生TabBar是合理的。医生端/管理员端有6+种角色需要动态TabBar，必须使用自定义方案。两种方案在同一项目中共存是合理的架构决策。

---

### D5: API接口一致性（10分）

#### 医生端：10/10

全部26个端点路径与API接口设计文档一致（含 `/api/v1` 前缀）。

**细节说明：**
- 路径变量名使用 `{id}` 而非API设计中的 `{appointmentId}/{childId}/{reactionId}` 等，这是文档表述差异，不影响实际URL路径
- 不良反应上报参数列表中未显式列出 `appointmentId`，但实际实现时通过路由参数传入是合理的
- 分页参数使用 `page/size`，与API设计一致

**问题：** 无

#### 家长端：10/10

全部23个端点路径与API接口设计文档完全一致。

| 模块 | 端点数 | 路径一致性 | 方法一致性 | 参数一致性 |
|------|--------|-----------|-----------|-----------|
| auth | 6 | ✓ | ✓ | ✓ |
| user | 2 | ✓ | ✓ | ✓ |
| child | 4 | ✓ | ✓ | ✓ |
| appointment | 6 | ✓ | ✓ | ✓ |
| vaccine | 1 | ✓ | ✓ | N/A |
| notice | 2 | ✓ | ✓ | ✓ |
| record | 2 | ✓ | ✓ | ✓ |

**问题：** 无

#### 管理员端：2/10

**系统性严重问题。** 全部34个端点存在路径不一致，具体如下：

**P0 [管理员端] API路径前缀缺失：** 全部端点缺少 `/api/v1` 前缀。-0.5×34 = 但上限为10分，实际影响：直接导致V3否决项触发。

**P0 [管理员端] API模块路径结构错误（5个模块）：**

| 前端API模块 | 前端路径 | API设计正确路径 | 问题 |
|------------|---------|---------------|------|
| schedule.js | `/api/schedule` | `/api/v1/admin/schedules` | 模块路径完全不同 |
| window.js | `/api/business/window` | `/api/v1/admin/windows` | 模块路径完全不同 |
| vaccine-manage.js | `/api/business/vaccine` | `/api/v1/admin/vaccines` | 模块路径完全不同 |
| notice-manage.js | `/api/business/notice` | `/api/v1/admin/notices` | 模块路径完全不同 |
| stats.js | `/api/admin/stats/*` | `/api/v1/admin/stats/*` | 仅缺少前缀 |

**P0 [管理员端] HTTP方法不一致：** 统计分析4个端点前端使用POST，API设计为GET。

**P0 [管理员端] 分页参数不一致：** 管理员端使用 `pageSize`，API设计使用 `size`。

**P0 [管理员端] API设计中不存在的前端端点：**
- `POST /api/schedule/conflict`（排班冲突检测）
- `POST /api/business/window/service`（配置窗口服务）
- `GET /api/business/window/service/{windowCode}`（查询窗口服务）
- `GET /api/admin/notice/{id}/feedback`（公告反馈列表）
- `DELETE /api/business/notice/{id}`（删除公告）
- `DELETE /api/business/vaccine/{id}`（删除疫苗）

**P0 [管理员端] 路径操作名不一致：**
- 前端 `PUT /api/admin/notice/{id}/approve` → API设计 `POST /api/v1/admin/notices/{id}/audit`
- 前端 `POST /api/admin/users/{userId}/roles` → API设计 `POST /api/v1/admin/users/{userId}/assign-roles`

**P1 [管理员端] API设计中存在但前端未调用的端点：**
- `PUT /api/v1/admin/vaccines/{vaccineId}/shelf-status`（疫苗上下架，前端用 `toggleVaccineStatus` → `PUT /api/business/vaccine/{id}/status`）
- `DELETE /api/v1/admin/roles/{roleId}`（删除角色）
- `POST /api/v1/admin/users/{userId}/freeze`（冻结用户）
- `POST /api/v1/admin/users/{userId}/unfreeze`（解冻用户）

---

### D6: 安全方案一致性（9分）

#### 医生端：8/9

| 检查项 | 架构设计§5 | 前端体现 | 状态 |
|--------|-----------|---------|------|
| JWT Token存储 | uni.setStorageSync('token', jwt) | 复用 utils/auth.js | ✓ |
| Token自动注入 | 请求拦截器注入 | 复用 request.js | ✓ |
| Token过期处理 | code=1001→清除Token→跳转登录 | 错误处理策略中列出 | ✓ |
| 注销流程 | Token加入Redis黑名单+清除前端状态 | logout() 调用后端API，前端清除状态 | ✓ |
| 防重放 | X-Request-Id (UUID v4) | 业务约束表列出 | ✓ |
| 角色路由守卫 | 登录后按roles跳转不同首页 | 动态TabBar配置 | ✓ |
| 动态TabBar权限 | 按角色显示不同Tab | CustomTabBar + Pinia | ✓ |
| 按钮级权限控制 | @PreAuthorize对应按钮显隐 | 部分描述（按角色显示操作） | **部分** |
| 敏感数据脱敏 | 手机号/身份证掩码 | 个人中心页脱敏描述 | ✓ |

**问题：**
- **P2 [医生端]** 按钮级权限控制未明确使用权限码（如 `appointment.signin`）做按钮显隐判断，仅按角色大类控制。建议在关键操作按钮上增加权限码判断描述。-1分

#### 家长端：9/9

家长端角色单一（USER），权限控制需求较低。所有安全机制均有体现：
- Token存储/注入/过期处理 ✓
- 注销流程 ✓
- 防重放（request.js自动生成X-Request-Id） ✓
- 路由守卫（公开/受保护页面分类清晰） ✓
- 敏感数据脱敏（手机号前3后4、身份证前3后4） ✓
- 冻结状态处理（预约冻结提示条、账号禁用强制退出） ✓

**问题：** 无

#### 管理员端：7/9

复用医生端安全方案，但描述不够详细。

**问题：**
- **P1 [管理员端]** Token过期处理和注销流程仅在错误处理策略表中列出1001/1002，未像医生端/家长端那样描述完整处理链路（清除状态→跳转登录页）。-1分
- **P2 [管理员端]** 防重放机制未在业务约束或API层设计中提及（如统计查询、排班创建等写操作是否需要X-Request-Id）。-1分

---

### D7: 数据模型一致性（8分）

#### 医生端：8/8

| 检查项 | 前端定义 | 后端定义 | 一致性 |
|--------|---------|---------|--------|
| 预约状态码（1/2/3/4/6/7/8/9/10） | 状态颜色映射表 | 共享内核状态机 | ✓ |
| 排队项状态码（0-4） | 叫号状态机描述 | 后端排队项状态 | ✓ |
| 批次状态（正常/即将过期/已过期/已销毁） | 批次状态颜色映射 | VaccineBatchStatus (0/1/2/3) | ✓ |
| 角色编码 | DOCTOR_SIGNIN等 | 共享内核10个角色 | ✓ |
| 疫苗类型 | 一类/二类 | CLASS_I/CLASS_II | ✓ |
| 不良反应枚举 | 局部/过敏/发热/其他 + 轻/中/重 | 共享内核定义 | ✓ |
| 错误码 | 3002/3001/3003/6002/6003/1005/5002/1001/1002 | 共享内核错误码体系 | ✓ |

**问题：** 无

#### 家长端：8/8

| 检查项 | 前端定义 | 后端定义 | 一致性 |
|--------|---------|---------|--------|
| 预约状态码 | 完整列出1-10所有状态 | 共享内核状态机 | ✓ |
| 疫苗类型 | CLASS_I/CLASS_II | 共享内核 | ✓ |
| 错误码映射 | 2005/2006/2007/2009/7001/7002/7007/7008/7011/7012/1011/1012 | 共享内核 | ✓ |

**问题：** 无

#### 管理员端：4/8

**系统性严重问题。** 多个状态码和枚举值与后端定义矛盾：

**P0 [管理员端] 公告状态码矛盾：**

| 状态码 | 管理员端定义 | 共享内核/API设计 |
|--------|------------|-----------------|
| 0 | 待审批 | 待审批 ✓ |
| 1 | 已审批 | **已发布**（PUBLISHED） |
| 2 | 已拒绝 | **已下架**（TAKEN_DOWN） |
| 3 | **缺失** | **已拒绝**（REJECTED） |

- "已审批"应为"已发布"
- 缺少状态码3（已拒绝）
- 状态码2语义完全不同

**P0 [管理员端] 公告类型枚举矛盾：**

| 管理员端 | 共享内核/API设计 |
|---------|-----------------|
| NOTICE | **NORMAL** |
| REMINDER | **缺失** |
| URGENT | URGENT ✓ |
| **缺失** | **SYSTEM** |

**P0 [管理员端] 排班状态码矛盾：**

| 状态码 | 管理员端定义 | 共享内核/API设计 |
|--------|------------|-----------------|
| 0 | 正常 | 正常 ✓ |
| 1 | 已取消 | **请假**（ON_LEAVE） |
| 2 | 已换班 | **已取消**（CANCELLED） |

- 状态码1和2的语义完全互换

**P0 [管理员端] 窗口职能类型多出SCHEDULE：**
- 管理员端列出 SIGNIN/PRECHECK/REGISTER/VACCINATE/OBSERVE/**SCHEDULE**
- 共享内核仅有 SIGNIN/PRECHECK/REGISTER/VACCINATE/OBSERVE
- SCHEDULE 不是窗口职能类型，而是独立的排班上下文

**P0 [管理员端] 错误码区间冲突：**
- 管理员端使用 5001-5055 区间
- 共享内核规定管理模块错误码为 8000-8999
- 5001-5006 已被登记模块（Register Module）占用
- 具体错误码对应关系完全错误（如管理员端5001"医生不存在"应为7003"USER_NOT_FOUND"）

---

### D8: 路由与页面完备性（8分）

#### 三端均：8/8

| 检查项 | 医生端 | 家长端 | 管理员端 |
|--------|--------|--------|----------|
| TabBar页面全部定义 | ✓ (11个) | ✓ (3个) | ✓ (4个) |
| 非Tab页面全部注册 | ✓ (17个) | ✓ (11个) | ✓ (17个) |
| 页面无孤立 | ✓ | ✓ | ✓ |
| 路径格式统一（kebab-case） | ✓ | ✓ | ✓ |
| 公共页面复用 | ✓ (auth, mine) | ✓ (auth, mine) | ✓ (auth, mine, index) |

**架构一致性说明：** 架构设计§4.2规划的路由前缀为 `/pages/guardian/*`、`/pages/doctor/*`、`/pages/admin/*`，但实际三端前端设计均使用扁平路径（如 `pages/queue/signin`、`pages/appointment/list`、`pages/admin/schedule/list`）。管理员端部分使用了 `pages/admin/*` 前缀，但医生端未使用 `pages/doctor/*` 前缀。这是实际设计与架构规划的小偏差，但由于三端共用同一项目，扁平路径避免了前缀冲突，属于合理调整。

**问题：** 无

---

### D9: 组件与交互完备性（8分）

#### 医生端：7/8

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 可复用组件清单 | ✓ | 6个组件（QueueCard, StatusTag, InfoCard, CountdownTimer, VerifyCheckbox, CustomTabBar） |
| 核心页面交互描述 | ✓ | 所有流程操作页有详细UI元素描述 |
| 表单校验规则 | ✓ | 关键表单有校验描述（如身份证18位、体温>37.3°C） |
| 列表交互 | ✓ | 下拉刷新、上拉加载、搜索、筛选 |
| 状态展示 | **部分** | 有操作反馈（Toast/确认弹窗），但缺少加载态（骨架屏）和空状态描述 |
| 操作反馈 | ✓ | 成功Toast、失败Toast、确认弹窗 |

**问题：**
- **P1 [医生端]** 缺少空状态处理描述（队列列表为空时显示什么）。-0.5分

#### 家长端：8/8

家长端组件和交互设计最为详尽：

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 可复用组件清单 | ✓ | 10个组件，每个有Props/Events/布局/样式规则 |
| 核心页面交互描述 | ✓ | 全部15个页面有ASCII线框图 |
| 表单校验规则 | ✓ | 登录/注册/儿童表单有完整校验规则表 |
| 列表交互 | ✓ | 下拉刷新、上拉加载、搜索、筛选 |
| 状态展示 | ✓ | 骨架屏规范、空状态组件（EmptyState）、网络断开浮层 |
| 操作反馈 | ✓ | 确认弹窗规范表（4种场景） |

**亮点：** 组件设计非常详细，每个组件有完整的Props定义、Events定义、ASCII布局图和样式规则。

**问题：** 无

#### 管理员端：7/8

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 可复用组件清单 | ✓ | 4个新增组件 + 3个复用组件 |
| 核心页面交互描述 | ✓ | 所有管理页面有功能描述 |
| 表单校验规则 | ✓ | 关键表单有校验描述 |
| 列表交互 | ✓ | FilterBar + 分页 |
| 状态展示 | **部分** | 缺少空状态和骨架屏描述 |
| 操作反馈 | ✓ | 二次确认弹窗（删除、审批） |

**问题：**
- **P1 [管理员端]** 缺少空状态处理描述（列表为空时的占位展示）。-0.5分

---

### D10: 异常处理完备性（7分）

#### 医生端：5/7

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 错误码映射表 | ✓ | 9个关键业务错误码有处理方式 |
| 网络异常处理 | ✓ | Toast + 手动刷新 |
| 空状态处理 | **缺失** | 未描述列表为空时的占位展示 |
| 边界场景 | ✓ | 并发叫号、重复提交（防重放）有描述 |
| Token异常处理 | ✓ | 1001→跳转登录，1002→Toast |
| 404/页面不存在 | **缺失** | 未描述无效路由的兜底处理 |

**问题：**
- **P1 [医生端]** 缺少空状态处理（队列列表、记录列表等为空时的展示）。-1分
- **P2 [医生端]** 缺少404页面兜底处理描述。-1分

#### 家长端：6/7

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 错误码映射表 | ✓ | 12个用户端错误码有处理方式 |
| 网络异常处理 | ✓ | 网络断开浮层 + 请求失败Toast + 重试 |
| 空状态处理 | ✓ | 5种页面空状态 + EmptyState组件 |
| 边界场景 | ✓ | 乐观更新（取消预约）、冻结状态、表单数据保留 |
| Token异常处理 | ✓ | 401/1001→跳转登录，来源页记录 |
| 404/页面不存在 | **缺失** | 未描述无效路由的兜底处理 |

**问题：**
- **P2 [家长端]** 缺少404页面兜底处理描述。-1分

#### 管理员端：3/7

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 错误码映射表 | **有但错误** | 错误码5001-5055与共享内核不匹配 |
| 网络异常处理 | ✓ | Toast + 手动刷新 |
| 空状态处理 | **缺失** | 未描述 |
| 边界场景 | **缺失** | 未描述并发操作、重复提交等场景 |
| Token异常处理 | ✓ | 1001→跳转登录 |
| 404/页面不存在 | **缺失** | 未描述 |

**问题：**
- **P0 [管理员端]** 错误码映射表中的错误码与共享内核定义完全不一致（详见D7）。-1分
- **P1 [管理员端]** 缺少空状态处理。-1分
- **P1 [管理员端]** 缺少边界场景描述。-1分
- **P2 [管理员端]** 缺少404页面兜底。-1分

---

### D11: 状态管理与轮询完备性（7分）

#### 医生端：7/7

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Store职责清晰 | ✓ | useUserStore/useQueueStore/useCallingStore职责分明 |
| 轮询间隔合理 | ✓ | 留观5s、叫号10s、队列30s、概览60s、库存不轮询 |
| 轮询生命周期管理 | ✓ | onShow启动、onHide/onUnload停止 |
| 轮询冲突处理 | ✓ | useQueue hook封装，页面切换自动清理 |
| 数据刷新策略 | ✓ | 下拉刷新、手动刷新、自动轮询各有适用场景 |
| Store持久化 | ✓ | Token通过uni.setStorageSync持久化 |
| 跨页面状态共享 | ✓ | 队列状态、叫号状态通过Store管理 |

**亮点：** 轮询策略表设计合理，不同业务场景使用不同间隔，库存低频页面使用手动刷新而非轮询。

**问题：** 无

#### 家长端：5/7

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Store职责清晰 | ✓ | useUserStore/useChildStore/useAppointmentStore |
| 轮询间隔合理 | ✓ | 流程指引30秒 |
| 轮询生命周期管理 | **部分** | 流程指引页有30秒轮询，但未明确描述onShow/onHide生命周期管理 |
| 轮询冲突处理 | **缺失** | 未描述 |
| 数据刷新策略 | ✓ | 下拉刷新、手动刷新 |
| Store持久化 | ✓ | Token通过auth.js持久化 |
| 跨页面状态共享 | ✓ | 预约状态通过Store管理 |

**问题：**
- **P1 [家长端]** 流程指引页30秒轮询未描述生命周期管理（onShow启动/onHide停止），可能导致页面切换后轮询继续运行。-1分
- **P2 [家长端]** 未描述轮询冲突处理（如快速切换预约详情和流程指引时）。-1分

#### 管理员端：5/7

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Store职责清晰 | ✓ | useUserStore扩展 + useAdminStore（筛选条件缓存） |
| 轮询间隔合理 | ✓ | 概览60秒、其他不轮询 |
| 轮询生命周期管理 | **部分** | 未明确描述 |
| 轮询冲突处理 | **缺失** | 管理端以手动刷新为主，冲突风险低 |
| 数据刷新策略 | ✓ | 手动下拉刷新 |
| Store持久化 | **缺失** | 未描述token/用户信息持久化方案 |
| 跨页面状态共享 | ✓ | 筛选条件通过useAdminStore缓存 |

**问题：**
- **P1 [管理员端]** Store持久化方案未描述（复用医生端应明确说明）。-1分
- **P2 [管理员端]** 轮询生命周期管理未明确描述。-1分

---

## 4. 问题汇总与优先级

### 4.1 按优先级汇总

#### P0（事实错误 — 必须修复）

| 编号 | 端 | 维度 | 问题描述 |
|------|----|------|---------|
| P0-1 | 管理员 | D5 | 全部API端点缺少 `/api/v1` 前缀 |
| P0-2 | 管理员 | D5 | 5个模块API路径结构与API设计完全不同（`/api/business/*` vs `/api/v1/admin/*`，`/api/schedule` vs `/api/v1/admin/schedules`） |
| P0-3 | 管理员 | D5 | 统计分析4个端点HTTP方法错误（POST应为GET） |
| P0-4 | 管理员 | D5 | 分页参数名不一致（`pageSize`应为`size`） |
| P0-5 | 管理员 | D7 | 公告状态码缺少状态3（已拒绝），状态1/2语义错误 |
| P0-6 | 管理员 | D7 | 公告类型枚举值与后端完全不同 |
| P0-7 | 管理员 | D7 | 排班状态码1和2语义互换 |
| P0-8 | 管理员 | D7 | 窗口职能类型多出不存在的SCHEDULE |
| P0-9 | 管理员 | D7 | 错误码使用5000区间，与共享内核8000区间规定冲突，且5000区间已被登记模块占用 |

#### P1（遗漏 — 必须修复）

| 编号 | 端 | 维度 | 问题描述 |
|------|----|------|---------|
| P1-1 | 医生端 | D10 | 缺少空状态处理描述 |
| P1-2 | 管理员 | D6 | Token过期处理和注销流程描述不完整 |
| P1-3 | 管理员 | D3 | 排班修改/删除的前置条件未在页面交互中体现 |
| P1-4 | 管理员 | D10 | 缺少空状态处理和边界场景描述 |
| P1-5 | 家长端 | D11 | 流程指引轮询未描述生命周期管理 |
| P1-6 | 管理员 | D11 | Store持久化方案未描述 |
| P1-7 | 管理员 | D9 | 缺少空状态占位描述 |

#### P2（不一致 — 应当修复）

| 编号 | 端 | 维度 | 问题描述 |
|------|----|------|---------|
| P2-1 | 三端 | D4 | UI组件库使用uni-ui而非架构规定的uView UI 2.x，未说明变更理由 |
| P2-2 | 医生端 | D6 | 按钮级权限控制未使用权限码 |
| P2-3 | 医生端 | D10 | 缺少404页面兜底 |
| P2-4 | 家长端 | D10 | 缺少404页面兜底 |
| P2-5 | 管理员 | D6 | 防重放机制未提及 |
| P2-6 | 管理员 | D10 | 缺少404页面兜底 |
| P2-7 | 管理员 | D11 | 轮询生命周期管理未明确 |
| P2-8 | 家长端 | D11 | 轮询冲突处理未描述 |

---

## 5. 各端评审结论

### 5.1 医生端APP前端设计 — S级（95分）

**优势：**
- PRD功能覆盖完整，无遗漏页面
- 业务流程链路清晰，叫号状态机设计精良
- API接口与后端设计完全一致
- 轮询策略设计合理，生命周期管理完善
- 防重放、二次确认等安全机制覆盖到位

**需修复：**
- P1-1: 补充队列列表/记录列表的空状态描述
- P2-2: 建议增加权限码级按钮控制描述
- P2-3: 补充404页面兜底处理

**结论：** 优秀。少量优化后可进入开发。

### 5.2 家长端APP前端设计 — S级（96分）

**优势：**
- 组件设计最为详尽（10个组件，每个有完整Props/Events/布局/样式定义）
- 全部15个页面有ASCII线框图，交互描述极为充分
- API接口与后端设计完全一致
- 错误处理体系完善（错误码映射、网络断开浮层、冻结状态处理、乐观更新）
- 表单校验规则有完整规则表
- UI视觉设计规范（配色、字体、间距、圆角、阴影）统一定义

**需修复：**
- P1-5: 流程指引页轮询需补充onShow/onHide生命周期管理描述
- P2-4: 补充404页面兜底处理

**结论：** 优秀，三端中设计最完善。少量优化后可进入开发。

### 5.3 管理员端APP前端设计 — B级（76分）

**优势：**
- 功能模块覆盖完整（排班、窗口、疫苗、公告、角色、配置、统计）
- 页面交互描述充分，业务约束表详尽（20条）
- 路由设计完整，无孤立页面
- 可复用组件设计合理（AdminNavCard、StatsCard、ChartPanel、FilterBar）

**严重问题（必须修复）：**
- **P0-1~4**: API层设计需要完全重写，对齐API接口设计文档的路径、方法、参数
- **P0-5~9**: 数据模型需要完全重写，对齐共享内核的状态码、枚举值、错误码

**修复建议：**
1. 将所有API路径加上 `/api/v1` 前缀
2. 将 `/api/business/*` 改为 `/api/v1/admin/*`，`/api/schedule` 改为 `/api/v1/admin/schedules`
3. 将统计查询HTTP方法从POST改为GET
4. 将分页参数 `pageSize` 统一改为 `size`
5. 对齐公告状态码（增加状态3已拒绝，状态1改为已发布）
6. 对齐公告类型枚举（NOTICE→NORMAL，REMINDER删除，增加SYSTEM）
7. 对齐排班状态码（1改为请假，2改为已取消）
8. 删除窗口职能SCHEDULE
9. 将错误码从5000区间改为8000区间，与共享内核对齐

**结论：** 合格但需重大修复。API层和数据模型需要重写对齐后可进入开发。

---

## 6. 评审通过标准检查

| 条件 | 要求 | 医生端 | 家长端 | 管理员端 |
|------|------|--------|--------|----------|
| 无一票否决项 | V1-V5全部未触发 | ✓ | ✓ | **不通过**（V3、V5） |
| 总分 ≥ 85 | A级以上 | ✓ (95) | ✓ (96) | **不通过** (76) |
| P0问题数 = 0 | 无事实错误 | ✓ | ✓ | **不通过**（9个P0） |
| P1问题修复率 = 100% | 全部遗漏已补齐 | 待修复 | 待修复 | 待修复 |

### 最终结论

| 端 | 评审结果 | 可否进入开发 |
|----|---------|------------|
| 医生端 | **评审通过**（修复P1后） | 是 |
| 家长端 | **评审通过**（修复P1后） | 是 |
| 管理员端 | **评审不通过**（需修复全部P0+P1后复审） | 否，需复审 |

## 7. 修复确认记录

| 修复日期 | 修复范围 | 修复问题编号 | 复审结果 |
|---------|---------|------------|---------|
| 2026-04-04 | Task 1: 管理员端API层重写（7个模块34个端点路径/方法/参数全面对齐） | P0-1, P0-2, P0-3, P0-4 | **通过** |
| 2026-04-04 | Task 2: 管理员端数据模型修复（公告状态码/类型枚举/排班状态码/窗口职能/错误码） | P0-5, P0-6, P0-7, P0-8, P0-9 | **通过** |
| 2026-04-04 | Task 3: 管理员端遗漏补充（安全方案/排班前置条件/空状态/边界场景/404/Store持久化/轮询生命周期） | P1-2, P1-3, P1-4, P1-6, P1-7, P2-5, P2-6, P2-7 | **通过** |
| 2026-04-04 | Task 4: 医生端补充（空状态/权限码级按钮控制/404兜底） | P1-1, P2-2, P2-3 | **通过** |
| 2026-04-04 | Task 5: 家长端补充（轮询生命周期/404兜底/轮询冲突处理） | P1-5, P2-4, P2-8 | **通过** |
| 2026-04-04 | Task 6: 三端技术栈变更说明（uni-ui变更理由） | P2-1 | **通过** |

### 8. 复审结果（2026-04-04）

#### 复审方法

对三份前端设计文档进行独立复审，验证所有24个问题（9 P0 + 7 P1 + 8 P2）是否已正确修复，同时检查修复是否引入新问题。

#### 8.1 修复验证结果

| 端 | P0通过 | P1通过 | P2通过 | 新发现问题 | 复审评级 |
|----|--------|--------|--------|-----------|---------|
| 管理员端 | 9/9 | 5/5 | 3/3 | 3个新P2 | **98/S 通过** |
| 医生端 | — | 1/1 | 2/2 | 0 | **99/S 通过** |
| 家长端 | — | 1/1 | 2/2 | 0 | **100/S 通过** |

#### 8.2 管理员端新发现P2问题（非阻塞）

| 编号 | 维度 | 问题描述 | 建议 |
|------|------|---------|------|
| NEW-P2-1 | D7 | TabBar配置示例代码中角色key使用 `BUSINESS_ADMIN` / `SCHEDULE_DOCTOR`，与共享内核角色编码 `DOCTOR_BUSINESS_ADMIN` / `DOCTOR_SCHEDULE` 不一致 | 对齐共享内核角色编码命名 |
| NEW-P2-2 | D7 | 错误码8015"不可修改超级管理员"未在共享内核中定义，共享内核中用户模块错误码7013为 `USER_UPDATE_SUPER_ADMIN_FAILED` | 改用7013或补充到共享内核 |
| NEW-P2-3 | D5 | 统计模块前端定义了4个查询端点（概览/接种效率/库存预警/异常统计），但API接口设计文档中仅定义了2个（概览+接种效率），库存预警和异常统计需后端补充API定义 | 与后端确认是否需要补充API |

#### 8.3 复审评分

| 维度 | 满分 | 医生端 | 家长端 | 管理员端 |
|------|------|--------|--------|----------|
| D1 PRD功能覆盖 | 12 | 12 | 12 | 12 |
| D2 页面流程覆盖 | 12 | 12 | 12 | 11 |
| D3 业务规则覆盖 | 11 | 11 | 11 | 11 |
| D4 技术选型一致性 | 8 | 8 | 8 | 8 |
| D5 API接口一致性 | 10 | 10 | 10 | 10 |
| D6 安全方案一致性 | 9 | 8 | 9 | 9 |
| D7 数据模型一致性 | 8 | 8 | 8 | 8 |
| D8 路由与页面完备性 | 8 | 8 | 8 | 8 |
| D9 组件与交互完备性 | 8 | 8 | 8 | 8 |
| D10 异常处理完备性 | 7 | 7 | 7 | 7 |
| D11 状态管理与轮询完备性 | 7 | 7 | 7 | 7 |
| **合计** | **100** | **99** | **100** | **98** |

#### 8.4 复审最终结论

| 端 | 复审总分 | 复审等级 | 一票否决项 | P0问题 | P1问题 | 结论 |
|----|---------|---------|-----------|--------|--------|------|
| 医生端 | 99 | **S** | 未触发 | 0 | 0 | **评审通过** |
| 家长端 | 100 | **S** | 未触发 | 0 | 0 | **评审通过** |
| 管理员端 | 98 | **S** | 未触发 | 0 | 0 | **评审通过** |

三端均达到评审通过标准（总分≥85、无一票否决项、P0问题数=0、P1问题修复率=100%），**可进入代码实施阶段**。

管理员端3个新P2问题为非阻塞项，不影响开发启动，建议在开发过程中同步处理。
